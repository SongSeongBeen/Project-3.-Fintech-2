spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  # Redis 자동 설정 비활성화 (개발 환경)
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  h2:
    console:
      enabled: true
      path: /h2-console


server:
  port: 8090
  servlet:
    encoding:
      charset: UTF-8
      force: true

jwt:
  secret: mySecretKey123456789012345678901234567890123456789012345678901234567890
  expiration:
    access: 30000       # Access Token 유효 시간 (30초 - 테스트용)
    refresh: 600000      # Refresh Token 유효 시간 (5분 - 테스트용)
  check:
    access: 10000        # 클라이언트 Access Token 체크 주기 (5초)
    background: 30000   # 클라이언트 백그라운드 체크 주기 (10초)

logging:
  level:
    org.springframework.security: DEBUG
    fintech2.easypay: DEBUG